rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Allow full read/write, since Firestore is 100% proxied behind our secure Express server
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
